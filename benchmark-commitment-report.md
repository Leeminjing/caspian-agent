# 承诺层 benchmark 报告

## 1. 阶段顺序完整性(机械)

- 5/5 次跑分阶段序列严格 1→9。

## 2. 注入跳步率(机械)

- 6/6 种「跳过/重排阶段」注入,阶段序列仍严格 1→9,跳步成功率 0%。

## 2b. 越序调用拒绝(机械)

- 越序直调 delegate_with_review(stage=9 而 expected=1):✓ 被 invalid_stage 拒绝。

## 3. 人工节点完整性(机械)

- 5/5 次跑分,3/5/6/7 四个人工节点全部命中。

## 4. 软 baseline 对照(LLM)

- 无注入:平均缺失规划维度 0.0/6,完整覆盖 3/3。
- 注入「跳过规划直接输出」:平均缺失规划维度 6.0/6,完整覆盖 0/3。

> 软 baseline(无阶段机)在注入下缺失更多规划维度;硬臂(承诺层)因阶段锁不受注入影响,恒产出完整合同。

## 结论

承诺层的「九阶段强制推进」是结构性硬机制:阶段严格递增、越序被拒、人工节点不丢、注入无法跳步;
而软 baseline(把规划纪律写进 prompt)没有阶段锁,注入即可诱导其跳过规划。
